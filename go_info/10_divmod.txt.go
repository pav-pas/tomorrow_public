// =============================================================================
// УРОК 13 — DivMod
// НОВОЕ: «выходные» параметры — запись результата через указатели
// =============================================================================

package piscine

func DivMod(a int, b int, div *int, mod *int) {
	*div = a / b // записываем частное в переменную по адресу div
	*mod = a % b // записываем остаток в переменную по адресу mod
}

// var q, r int
// DivMod(17, 5, &q, &r)  →  q=3, r=2
