// =============================================================================
// УРОК 9 — Swap
// НОВОЕ: обмен значений через указатели (практика & и *)
// =============================================================================

package piscine

func Swap(a *int, b *int) {
	c := *a  // временная переменная
	*a = *b
	*b = c
}

// x, y := 3, 7  →  Swap(&x, &y)  →  x=7, y=3
